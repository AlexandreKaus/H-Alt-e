<% @alternatives.each do |alternative| %>

<div class="container">
  <div class="card-alternatives-container">
      <div class="index-img">
      </div>
      <div class="card-alternatives-left">
<h2> <%= link_to alternative.title, alternative_path(alternative) %> </h2>
<div class="property"> <%= alternative.difficulty %> </div>
<p> <%= alternative.description %> </p>
    </div>
      <div class="separator"></div>
    <div class="card-alternatives-right">
    <div class="property">D.I.Y. </div>
<p> <i class="far fa-clock"></i><%= alternative.prep_time %> min.</p>
    <% alternative.photos.each do |pic| %>
      <%= cl_image_tag pic.photo, width: 100, height: 100 %>
    <% end %>
    </div>
  </div>
</div>
<div class="white-line"></div>
<% end %>
